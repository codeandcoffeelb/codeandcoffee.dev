---
import Layout from './Layout.astro';

export interface Props {
	title: string;
}

const { title } = Astro.props;
---

<Layout title={title}>
	<div class="wrapper">
		<slot />
	</div>
</Layout>

<style lang="scss">
	.wrapper {
		max-width: 800px;
		margin: 0 auto;
	}
</style>